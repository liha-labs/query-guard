{
  "name": "query-guard-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev:site": "pnpm -C apps/site dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
